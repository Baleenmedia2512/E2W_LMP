/**
 * API Route Configuration
 * Forces all API routes to be dynamic (runtime-only)
 */

export const runtime = 'nodejs';
export const dynamic = 'force-dynamic';
export const dynamicParams = true;
export const revalidate = 0;
